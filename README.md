# RL_Q-Learning_Agent
A Q-learning agent trained in a 10×10 grid world to reach a target while avoiding an enemy. The agent learns an optimal navigation policy using an epsilon-greedy strategy, reward shaping, and Q-table updates based on relative object positions. Training progress is evaluated through success rates and visualized with OpenCV. 
